version: "3.9"

services:
  streamlit-dashboard:
    build:
      dockerfile: Dockerfile
    volumes:
      - ".:/usr/src/app"
    ports:
      - "8501:8501"
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=root
      